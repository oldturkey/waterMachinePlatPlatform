<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.terabits.mapper.PresentMapper">

    <select id="sumPresentByPhoneAndTime" resultType="java.lang.Double">
        SELECT CASE WHEN SUM(money) IS NULL THEN 0 ELSE SUM(money) END FROM present
        <where>
            <if test="param1 != null and param1 != ''">
                and phone = #{param1}
            </if>
            <if test="param2.beginTime != null and param2.beginTime != ''">
                and gmtcreate >= #{param2.beginTime}
            </if>
            <if test="param2.endTime != null and param2.endTime != ''">
                and gmtcreate &lt;= #{param2.endTime}
            </if>
        </where>
    </select>

</mapper>